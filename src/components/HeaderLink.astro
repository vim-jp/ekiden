---
type Props = { slug: string; href: string };
const { slug, href } = Astro.props;

const isLink = slug !== href;
const Tag = isLink ? "a" : "span";
const props = isLink ? { href: `/ekiden${href}` } : {};
---

<span class="px-4 py-2">
  <Tag class="px-4 py-2 underline" {...props}>
    <slot />
  </Tag>
</span>
